#include <bits/stdc++.h>
using namespace std;
int main() 
{
    string are="41325";
    // two very important functions in c++.
    sort(are.begin(),are.end());
    reverse(are.begin(),are.end());
    cout<<are<<endl;
return 0 ;
}